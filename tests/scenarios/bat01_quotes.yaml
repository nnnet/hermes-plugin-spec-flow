# GENERATED by tests/lib/battery_gen.py — DO NOT EDIT BY HAND.
# Universality battery spec (node F1): seed=20260705 index=0 domain=quotes kind=service.
# Regenerate: python3 tests/lib/battery_gen.py --write (audit S20.2 pins the bytes).
# Body is JSON (a YAML subset) so generation stays stdlib-only and byte-stable.
{
  "name": "bat01_quotes",
  "node_engine": "fsm",
  "goal": "A tiny quote keeper service over WSGI, standard library only. It stores quotes in a small file-backed store and serves them over HTTP exactly as the frozen contract states: create via POST /quotes, list via GET /quotes (newest first). No extras required.",
  "target": "Binary success: the assembled product boots in a fresh process and every endpoint frozen in the constitution answers exactly as stated; the acceptance suite is green.",
  "policy": {
    "measurable_target": true,
    "spend_per_action_usd": 0,
    "human_in_loop": true,
    "involves_outreach": false,
    "consent_obtained": true,
    "legal_exposure": false,
    "legality_reviewed": true
  },
  "constitution": [
    "GET /health responds 200 with JSON {\"status\": \"ok\"}.",
    "POST /quotes takes a JSON body with required fields {\"quote\", \"speaker\", \"source\"} and responds 200 with JSON {\"id\": <int>}; a body missing a required field responds 400.",
    "GET /quotes responds 200 with JSON {\"items\": [...]} where each item carries \"id\" and the stored fields, newest first.",
    "DELETE /quotes removes every stored quote and responds 200 with JSON {\"cleared\": true}.",
    "Every test sets the QUOTES_DB env var to a fresh temp path BEFORE connecting; the store reads it per call.",
    "Module ownership is FIXED: the quote store lives in src/quote_store.py; the HTTP handlers live in src/quotes_api.py. The engine owns the WSGI entry point.",
    "Standard library only — no third-party packages."
  ],
  "hitl": {
    "approve_required": false,
    "worker_may_ask_human": true,
    "human_may_intervene": true
  },
  "doctor": {
    "enabled": true
  },
  "causes": {
    "empty_delta": {
      "ladder": [
        "reject_empty",
        "rework",
        "escalate_tier"
      ]
    },
    "task_too_large": {
      "ladder": [
        "split",
        "record"
      ]
    }
  },
  "evaluator": {
    "tier": "strong",
    "temperature": 0.0,
    "votes": 1,
    "require_cite": true
  },
  "review": {
    "on_reject": "rework",
    "max_rework": 2
  },
  "integrate": {
    "pre_gate": true
  },
  "acceptance": {
    "smoke": [
      "service starts and answers GET /health -> 200"
    ],
    "e2e": [
      "POST /quotes {\"quote\": \"a quote\", \"speaker\": \"a speaker\", \"source\": \"a source\"} -> {id}; GET /quotes -> items contains that quote",
      "two POSTs then GET /quotes -> the newest created quote appears first",
      "after a POST, DELETE /quotes then GET /quotes -> items is empty"
    ]
  },
  "workers": {
    "backend": "openai",
    "base_url": "http://127.0.0.1:8080/v1",
    "claude_gateway": {
      "base_url": "http://127.0.0.1:8080/v1",
      "model_map": {
        "claude/sonnet": "anthropic/claude-sonnet-4-6",
        "sonnet": "anthropic/claude-sonnet-4-6"
      }
    },
    "retries": 6,
    "timeout": 75,
    "leaf_depth": 3,
    "max_children": 3,
    "max_decompose_calls": 40,
    "creator_ensemble": 2,
    "max_concurrent_llm_requests": 3,
    "provider_concurrency": {
      "claude": 1,
      "anthropic": 1
    },
    "provider_timeout": {
      "claude": 180,
      "anthropic": 180
    },
    "cycle_models": false,
    "quota_wait_s": 20,
    "quota_retries": 3,
    "fallback_models": [
      "claude/sonnet"
    ],
    "tiers": {
      "weak": {
        "models": [
          "xiaomimimo/mimo-v2.5",
          "claude/sonnet"
        ],
        "temperature": 0.2
      },
      "medium": {
        "models": [
          "xiaomimimo/mimo-v2.5",
          "claude/sonnet"
        ],
        "temperature": 0.1
      },
      "strong": {
        "models": [
          "xiaomimimo/mimo-v2.5-pro",
          "claude/sonnet"
        ],
        "temperature": 0.0
      }
    },
    "complexity_to_tier": {
      "leaf_small": "weak",
      "leaf_big": "medium",
      "branch": "strong"
    },
    "providers": [
      {
        "name": "xiaomimimo",
        "kind": "openai",
        "model_prefix": "xiaomimimo/",
        "requests_per_day": 1000
      },
      {
        "name": "claude-cli",
        "kind": "claude",
        "model_prefix": "claude/"
      }
    ],
    "defaults": {
      "models": [
        "xiaomimimo/mimo-v2.5",
        "claude/sonnet",
        "xiaomimimo/mimo-v2.5-pro"
      ]
    },
    "stages": {
      "decompose": {
        "models": [
          "xiaomimimo/mimo-v2.5",
          "claude/sonnet"
        ]
      },
      "implement": {
        "models": [
          "xiaomimimo/mimo-v2.5",
          "claude/sonnet"
        ]
      },
      "review": {
        "models": [
          "xiaomimimo/mimo-v2.5",
          "claude/sonnet"
        ],
        "params": {
          "reasoning": "off"
        }
      },
      "verify": {
        "models": [
          "xiaomimimo/mimo-v2.5-pro",
          "claude/sonnet"
        ],
        "params": {
          "reasoning": "off"
        }
      }
    }
  }
}
