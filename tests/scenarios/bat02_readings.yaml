# GENERATED by tests/lib/battery_gen.py — DO NOT EDIT BY HAND.
# Universality battery spec (node F1): seed=20260705 index=1 domain=readings kind=service.
# Regenerate: python3 tests/lib/battery_gen.py --write (audit S20.2 pins the bytes).
# Body is JSON (a YAML subset) so generation stays stdlib-only and byte-stable.
{
  "name": "bat02_readings",
  "node_engine": "fsm",
  "goal": "A tiny reading keeper service over WSGI, standard library only. It stores readings in a small file-backed store and serves them over HTTP exactly as the frozen contract states: create via POST /readings, list via GET /readings (oldest first), plus an HTML page at /about. No extras required.",
  "target": "Binary success: the assembled product boots in a fresh process and every endpoint frozen in the constitution answers exactly as stated; the acceptance suite is green.",
  "policy": {
    "measurable_target": true,
    "spend_per_action_usd": 0,
    "human_in_loop": true,
    "involves_outreach": false,
    "consent_obtained": true,
    "legal_exposure": false,
    "legality_reviewed": true
  },
  "constitution": [
    "GET /health responds 200 with JSON {\"status\": \"ok\"}.",
    "POST /readings takes a JSON body with required fields {\"sensor\", \"value\"} and responds 200 with JSON {\"id\": <int>}; a body missing a required field responds 400.",
    "GET /readings responds 200 with JSON {\"items\": [...]} where each item carries \"id\" and the stored fields, oldest first.",
    "GET /readings/count responds 200 with JSON {\"count\": <int>} — the number of stored readings.",
    "GET /about responds 200 with a server-rendered HTML page (text/html) describing what this sensor reading log records; standard library only, no JavaScript frameworks.",
    "Every test sets the READINGS_DB env var to a fresh temp path BEFORE connecting; the store reads it per call.",
    "Module ownership is FIXED: the reading store lives in src/reading_store.py; the HTTP handlers live in src/readings_api.py; the /about page lives in src/readings_page.py. The engine owns the WSGI entry point.",
    "Standard library only — no third-party packages."
  ],
  "hitl": {
    "approve_required": false,
    "worker_may_ask_human": true,
    "human_may_intervene": true
  },
  "doctor": {
    "enabled": true
  },
  "causes": {
    "empty_delta": {
      "ladder": [
        "reject_empty",
        "rework",
        "escalate_tier"
      ]
    },
    "task_too_large": {
      "ladder": [
        "split",
        "record"
      ]
    }
  },
  "evaluator": {
    "tier": "strong",
    "temperature": 0.0,
    "votes": 1,
    "require_cite": true
  },
  "review": {
    "on_reject": "rework",
    "max_rework": 2
  },
  "integrate": {
    "pre_gate": true
  },
  "acceptance": {
    "smoke": [
      "service starts and answers GET /health -> 200"
    ],
    "e2e": [
      "POST /readings {\"sensor\": \"a sensor\", \"value\": \"a value\"} -> {id}; GET /readings -> items contains that reading",
      "two POSTs then GET /readings -> the oldest created reading appears first",
      "after two POSTs, GET /readings/count -> {\"count\": 2}"
    ]
  },
  "workers": {
    "backend": "openai",
    "base_url": "http://127.0.0.1:8080/v1",
    "claude_gateway": {
      "base_url": "http://127.0.0.1:8080/v1",
      "model_map": {
        "claude/sonnet": "anthropic/claude-sonnet-4-6",
        "sonnet": "anthropic/claude-sonnet-4-6"
      }
    },
    "retries": 6,
    "timeout": 75,
    "leaf_depth": 3,
    "max_children": 3,
    "max_decompose_calls": 40,
    "creator_ensemble": 2,
    "max_concurrent_llm_requests": 3,
    "provider_concurrency": {
      "claude": 1,
      "anthropic": 1
    },
    "provider_timeout": {
      "claude": 180,
      "anthropic": 180
    },
    "cycle_models": false,
    "quota_wait_s": 20,
    "quota_retries": 3,
    "fallback_models": [
      "claude/sonnet"
    ],
    "tiers": {
      "weak": {
        "models": [
          "xiaomimimo/mimo-v2.5",
          "claude/sonnet"
        ],
        "temperature": 0.2
      },
      "medium": {
        "models": [
          "xiaomimimo/mimo-v2.5",
          "claude/sonnet"
        ],
        "temperature": 0.1
      },
      "strong": {
        "models": [
          "xiaomimimo/mimo-v2.5-pro",
          "claude/sonnet"
        ],
        "temperature": 0.0
      }
    },
    "complexity_to_tier": {
      "leaf_small": "weak",
      "leaf_big": "medium",
      "branch": "strong"
    },
    "providers": [
      {
        "name": "xiaomimimo",
        "kind": "openai",
        "model_prefix": "xiaomimimo/",
        "requests_per_day": 1000
      },
      {
        "name": "claude-cli",
        "kind": "claude",
        "model_prefix": "claude/"
      }
    ],
    "defaults": {
      "models": [
        "xiaomimimo/mimo-v2.5",
        "claude/sonnet",
        "xiaomimimo/mimo-v2.5-pro"
      ]
    },
    "stages": {
      "decompose": {
        "models": [
          "xiaomimimo/mimo-v2.5",
          "claude/sonnet"
        ]
      },
      "implement": {
        "models": [
          "xiaomimimo/mimo-v2.5",
          "claude/sonnet"
        ]
      },
      "review": {
        "models": [
          "xiaomimimo/mimo-v2.5",
          "claude/sonnet"
        ],
        "params": {
          "reasoning": "off"
        }
      },
      "verify": {
        "models": [
          "xiaomimimo/mimo-v2.5-pro",
          "claude/sonnet"
        ],
        "params": {
          "reasoning": "off"
        }
      }
    }
  }
}
