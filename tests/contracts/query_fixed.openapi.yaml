openapi: 3.0.0
info: {title: Analytics query API, version: 1.1.0}
paths:
  /query:
    get:
      responses:
        '200':
          content:
            application/json:
              schema:
                properties:
                  series: {type: string}
                  count: {type: string}
