# Общий движок: убрать HTTP-центризм, решать задачу для ЛЮБОГО сервиса

> Контекст: `.claude/HANDOFF.md`. Ветка `feat/roadmap-phase-1`.
> Задача движка (медиум-независимо): из описания → детальные спеки → граф мелких
> узлов → ДЕТЕРМИНИРОВАННО собрать сервис без угадывания. HTTP — ЧАСТНЫЙ случай.

## Уже сделано (корень «ни один прогон не зелёный»)
- **#128** (`8b70933`) — детерм. репейр импортов: `from X import Y` → реальный
  владелец Y. Закрывает подкласс «правильный символ, не тот модуль». Медиум-независимо.
- **#113 слайс 1** (`400292c`) — `_available_interfaces` (AST-скан собранных
  модулей → символы+сигнатуры) публикуется в контекст ВСЕХ путей кодера
  (оркестр+solo-claude; solo-chat уже имел repo_map). Кодер видит реальный
  интерфейс соседа → не выдумывает имена. Закрывает подкласс «выдуманное имя»
  на входе; остаток ловит гейт → доктор.

## Остаётся (медиум-независимая перестройка, слайсами)

### Слайс 2 — типизированные рёбра графа зависимостей ✅ (`92bea1f`)
СДЕЛАНО: карточка узла получила `exposes:[символ]` + `needs:[{from,symbols}]`
(схема декомпозера просит); `_interface_edge_violations(tree)` — чистый гейт
плана (символ needs обязан быть в exposes производителя, иначе битое ребро до
сборки), диагностикой в plan-check, ленив к пустым (back-compat); declared
контракт кормится кодеру через `_interfaces_block` (вместе с реальными
интерфейсами #113с1). 10 офлайн-тестов вкл не-веб. Медиум-независимо.

### Слайс 3/4 — ЯДРА + ИНТЕГРАЦИЯ ✅ (`cfb8f6b` ядра, `d73359d` интеграция)
ЯДРА (чистые функции, 6 тестов): `_project_kind(texts)` (web|cli|lib|''),
`_synthesize_lib_entry(owner_by_symbol)` (реэкспорт API — не-веб аналог WSGI-роутера),
`_capability_probe_src(entry, symbol)` (приёмка по поведению — не-веб аналог boot-gate).
ИНТЕГРАЦИЯ ВШИТА (`d73359d`): `_product_contract` при отсутствии HTTP-роутов выводит
форму через `_project_kind` и возвращает медиум-независимый контракт
`{kind, entry, exposes}` без routes/boot (веб-путь не тронут — routes выигрывают
первыми; p4/p5/p6 объявляют routes → в ветку не заходят); `_try_synthesize_lib_entry`
(новый) — детерм. сборка не-веб входа по AST-реестру символов, реэкспорт API,
отказ (LLM остаётся ответствен) когда способность не собрана; `_try_synthesize_entry`
ветвит на неё по kind; `_nonweb_capability_boots` (новый) — root-гейт по поведению
(импорт входа в герметичном подпроцессе + capability-probe); `_assembled_product_boots`
и `_assembly_node` ветвят по kind (не-веб → без LLM assembly-листа). Офлайн 487 зелёных.

### Слайс 3 (описание) — точка входа по ФОРМЕ проекта (не всегда WSGI)
`_synthesize_entry_code` сейчас всегда строит WSGI-роутер. Обобщить: форма входа
выводится из спеки — `main()`-диспетчер (CLI), реэкспорт в `__init__` (библиотека),
раннер (джоб/конвейер), WSGI (веб). Либо: точка входа = обычный узел с контрактом
«выставить верхнюю описанную способность». Контракт (`_product_contract`) даёт
`entry`+`exposes`; добавить `kind` (cli|lib|web|job). Не-веб проект получает
верную точку входа.

### Слайс 4 — приёмка по ПОВЕДЕНИЮ (не только curl HTTP)
boot-gate сейчас курлит `/health`. Обобщить: приёмка = исполнить ОПИСАННОЕ
поведение через собранный код. Для веба — HTTP-вызов (частный адаптер); для
CLI — запустить команду и проверить вывод; для библиотеки — импортировать и
вызвать API, проверить результат. Источник истины — контракт-приёмка (детерм.,
из спеки), а не сгенерированные юнит-тесты. Файлы: pytest_verifier boot-gate,
contract_checks.

### Слайс 5 — не-веб E2E кейс ✅ создан (`d73359d`), живой прогон остаётся
СДЕЛАНО: `tests/scenarios/p7_word_stats_lib.yaml` — библиотека статистики слов БЕЗ
HTTP (`kind=lib`, `exposes word_count/split_words`, `integrate.pre_gate: true` →
capability boot-gate = честный пол). Плюс офлайн-тесты вшивки + защита формулировок
от дрейфа (`tests/coverage/test_nonweb_pipeline_wiring.py`, 6 тестов). Раннер видит
кейс (`run_cases.py` глобит `scenarios/*.yaml`; `--case p7`). ОСТАЁТСЯ: живой
отвязанный прогон `--case p7_word_stats_lib --depth product` до `meta.status=READY`
(заблокирован провайдером — claude через Bifrost→Meridian отдаёт HTTP 400 на
preflight; спросить юзера про рестарт Meridian).

## Правила
- Медиум-независимо: ничего HTTP-специфичного в ядре; веб — адаптер.
- Детерминированно кодом, промпт дублирует, гейт проверяет (не промпт-онли).
- Не ослаблять honest-гейты. Реальные баги чинит система (доктор), движок — мой.
- Офлайн зелёный → коммит → пуш. Тесты — free OpenRouter через Bifrost; claude
  подписочный. Прогоны отвязанно.

## Валидация всего
Свежий p6 (веб) при спокойном free-пуле до зелёного `meta.status=READY` +
не-веб кейс (слайс 5) до READY. Оба зелёных = движок реально общий, не веб-частный.
