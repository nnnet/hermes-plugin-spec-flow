# spec-flow — план развития, архитектурные решения и полный функционал

Дата: 2026-06-11. Источник требований: `docs/conversation.md` (вся переписка
о Spec-Driven Development в Hermes). Веб-сверка фреймворков: octябрь 2025 —
июнь 2026 (Martin Fowler «SDD: Kiro, spec-kit, Tessl»; cameronsjo/spec-compare;
openspec.pro/comparison; github/spec-kit v0.10, OpenSpec v1.3, BMad v6.8).

---

## Вопрос 1 — свои скиллы/профили или сторонние опенсорс-библиотеки?

### Что есть на рынке (проверено в вебе)

| Инструмент | Лицензия | Чем силён | Чем не подходит как замена |
|---|---|---|---|
| GitHub **spec-kit** | MIT | constitution → specify → plan → tasks → implement; 30+ агентов; ставится в skills-режиме | spec-**first** (спека живёт на время одной задачи, не фичи), нет durable-оркестрации, нет drift/respec |
| AWS **Kiro** | проприетарный | EARS-требования, трассировка req→task, IDE+хуки | привязка к AWS Bedrock и моделям, своя IDE, кредиты |
| **OpenSpec** | MIT | brownfield, дельта-формат ADDED/MODIFIED/REMOVED, лёгкий | нет оркестрации параллельных воркеров, нет гейтов глубины |
| **BMad** | MIT | 21 агент-роль, enterprise | тяжёлый, не про Kanban-исполнение |
| **specmatic / redocly / buf / tsc** | OSS | реальная валидация контрактов OpenAPI/Zod/Protobuf | это валидаторы, не методология |

### Вывод (обоснованный, с + и −)

**Свои скиллы/профили — оставляем. Свои валидаторы контрактов — НЕ пишем,
берём готовые. Спеку-«фронт» — поддерживаем как опциональный вход, не替.**

Почему именно так — граница ответственности (она же зафиксирована в
`conversation.md`, строки 736, 786): *spec-kit/Kiro сильны в генерации
артефактов спеки и гейтах качества; Hermes-Kanban силён в durable-оркестрации
параллельных воркеров с ретраями и human-in-the-loop. Продуктивная связка —
спека снаружи, исполнение и трекинг на Kanban.*

**За свои скиллы/профили (+):**
- наши 9 скиллов — это «движок дисциплины» (рекурсивные гейты глубины,
  drift→respec, две линии ревизии), которого нет ни у одного из фреймворков:
  они спека-first, а у нас спека-**anchored** на весь срок фичи с
  версионированием и пересборкой поддерева;
- скилл — markdown в том же agent-loop, ноль рантайм-зависимостей,
  работает офлайн (доказано: 143 теста без Hermes);
- профили дают физическую изоляцию ролей (декомпозитор не умеет кодить) —
  у фреймворков-CLI этого нет.

**Против своих (−):**
- мы не должны конкурировать с генераторами спек и валидаторами —
  это чужая, уже решённая работа;
- риск «изобрести EARS заново» — берём нотацию EARS как есть (она в spec-kit/Kiro).

**Что берём извне (готовые либы), а не пишем сами:**
1. **Валидаторы контрактов** — `redocly`/`specmatic` (OpenAPI), `tsc` (Zod),
   `buf` (Protobuf). Уже заложено: таблица `CONTRACT_VALIDATORS`. Свой
   `openapi_diff.py` — только тестовая заглушка, в проде подменяется.
2. **Спека-«фронт» (опционально)** — spec-kit / OpenSpec как источник
   `spec.md`/`tasks.md`. Наш L0-декомпозер читает их артефакты как вход
   (`Traces-to` → требования из `spec.md`). Не替, а адаптер.
3. **EARS-нотация** — как соглашение в `spec-requirements`, не своя выдумка.
4. **Браузер/поиск для research-линии** — MCP-серверы Hermes (`browser`,
   `search`), а не свой веб-клиент.

---

## Вопрос 2 — нужна ли стейт-машина (`engines/workflow-engine`)?

### Что такое workflow-engine

Универсальный движок стейт-машины на `pytransitions`: воркфлоу декларирует
**фазы + переходы + decide_fn + промпт на фазу**, движок детерминированно
ведёт это по ходам (персист состояния, маршрутизация фаз, guard'ы
обязательных фраз). Ядро `core/` не содержит доменных строк — всё подаётся
воркфлоу. Уже используется плагином `desire-to-goal-driver`.

### Вывод (обоснованный, с + и −)

**Да, для ОДНОГО узла (life-cycle листа/ветки) стейт-машина оправдана и
снимает реальный риск. Для дерева в целом — НЕТ, там Kanban-DAG, не FSM.**

Сейчас порядок фаз узла (requirements → decompose → contract → impl → review →
integrate, с циклами clarify/critique/drift/respec) зашит императивно в
`spec_flow_runner.py` (методы `_visit`/`_leaf_pipeline`). Это работает, но
порядок и гейты живут в коде Python, а в Hermes — в markdown-скиллах, и они
могут разойтись (LLM пропустит фазу, выдумает lock-сигнал — ровно та боль,
ради которой workflow-engine и создан, см. его README).

**За стейт-машину на уровне узла (+):**
- порядок фаз и обязательные гейты становятся **данными** (одна декларация),
  а не дублируются в коде раннера и в тексте скиллов → один источник истины;
- движок уже даёт персист состояния по ходам, guard'ы «обязательной фразы»
  (нельзя закрыть узел без contract_check/review) — это наши инварианты
  R1–R9, но проверяемые рантаймом, а не только пост-фактум аудитом;
- циклы (clarify-loop, critique-loop, drift→respec) — это ровно
  «cycles/branching», под которые движок и сделан;
- переиспользуем уже существующий плагин коллекции, а не плодим свой FSM.

**Против / ограничения (−):**
- **дерево** задач — это DAG с произвольной глубиной и параллелизмом; FSM
  его не моделирует. Дерево остаётся на Kanban (durable, параллельно,
  переживает рестарты). FSM — только внутренний жизненный цикл ОДНОГО узла;
- зависимость от `pytransitions` (внешний пакет) в рантайме узла — против
  нынешней нулевой-зависимости. Митигируется: FSM-режим опционален, дефолт —
  текущий императивный раннер (он уже оттестирован 143 тестами);
- цена интеграции: переписать `_leaf_pipeline` как воркфлоу-конфиг.

**Решение:** ввести FSM **опционально**, как второй движок жизненного цикла
узла (`node_engine="fsm"|"inline"`, дефолт `inline`). Дерево/Kanban не трогаем.

---

## ПОЛНЫЙ ФУНКЦИОНАЛ (всё, что обсуждалось в conversation.md)

Легенда средств: **[своё]** — наш код/скилл; **[либа]** — сторонняя
библиотека/сервис; **[движок]** — `engines/workflow-engine`; **[Hermes]** —
штатный механизм Hermes (Kanban, профили, MCP).

### A. Ядро методологии (уже есть — поддерживаем)

| # | Функция | Статус | Средства |
|--:|---|---|---|
| A1 | 4 эшелона L1 req → L2 contract → L3 plan → L4 atomic | ✅ есть | **[своё]** `spec-flow-decompose` |
| A2 | Top-down декомпозиция с гейтом глубины `leaf_check` | ✅ есть | **[своё]** тул `leaf_check` |
| A3 | Контракт L2 как артефакт (OpenAPI деф., Zod/Protobuf, параллельно) | ✅ есть | **[своё]** `spec-contract` + тул `contract_check`; **[либа]** redocly/specmatic/tsc/buf |
| A4 | Авто-детект дрейфа код↔контракт | ✅ есть | **[своё]** `drift-gate` |
| A5 | Инверсия «спека-первой» + версионирование | ✅ есть | **[своё]** `respec-gate` |
| A6 | Spike-before-freeze (ресёрч до заморозки) | ✅ есть | **[своё]** `spec-research` (режим spike) |
| A7 | Continuous revision lane (триггеры every_n/m_errors/on_return/cron) | ✅ есть | **[своё]** тул `research_trigger_check` |
| A8 | Bottom-up интеграция + параллельный contract_check поддерева | ✅ есть | **[своё]** `spec-integrate` |
| A9 | 6 профилей с урезанными toolset (изоляция ролей) | ✅ есть | **[своё]**+**[Hermes]** профили |
| A10 | policy_gate — ловля размытых/рискованных постановок | ✅ есть | **[своё]** тул `policy_gate` |
| A11 | run_report — footprint + аудит R1–R9, покрытие из лога | ✅ есть | **[своё]** `build_run_report` |
| A12 | Плагин сам строит дерево из цели (decomposer-агент) | ✅ есть | **[своё]** движок + **[либа]** LLM (claude CLI/SDK) |

### B. Связка с внешними SDD-стандартами (новое — адаптеры, не замена)

| # | Функция | Средства |
|--:|---|---|
| B1 | Импорт `tasks.md`/`spec.md` от **spec-kit** → карточки Kanban с `--parent` по зависимостям (аналог `taskstoissues`, цель — Kanban) | **[либа]** spec-kit как фронт + **[своё]** парсер→`kanban_create` |
| B2 | Импорт Kiro-артефактов (requirements/design/tasks .md) в `dir:`-workspace как контекст | **[либа]** Kiro + **[своё]** загрузчик |
| B3 | Импорт **OpenSpec** дельт (ADDED/MODIFIED/REMOVED) для brownfield-изменений → respec-узлы | **[либа]** OpenSpec + **[своё]** маппер дельт→respec |
| B4 | EARS-нотация требований в `spec-requirements` | **[либа]** EARS (соглашение) + **[своё]** скилл |
| B5 | openapi-mcp-generator: контракт → готовый MCP-сервер инструментов для узлов, работающих с этим API | **[либа]** openapi-mcp + **[Hermes]** подключение MCP |

### C. Жизненный цикл узла на стейт-машине (новое — опционально)

| # | Функция | Средства |
|--:|---|---|
| C1 | Фазы узла (req→decompose→contract→impl→review→integrate) как воркфлоу-конфиг | **[движок]** workflow-engine + **[своё]** WorkflowConfig |
| C2 | Циклы clarify/critique/drift→respec как переходы FSM | **[движок]** transitions |
| C3 | Guard'ы инвариантов R1–R9 как mandatory-phrase guards (рантайм, не только аудит) | **[движок]** + **[своё]** validators |
| C4 | Персист состояния узла по ходам (переживает рестарт воркера) | **[движок]** state.py + **[Hermes]** Kanban runs |

### D. Полный цикл разработки до готового продукта (новое — из плана readiness)

| # | Функция | Средства |
|--:|---|---|
| D1 | Уровни глубины spec→scaffold→verify→execute | ✅ есть **[своё]** |
| D2 | Живой implementer-агент (настоящий код листа) | **[либа]** Hermes-воркер / Claude Agent SDK + **[своё]** адаптер |
| D3 | Уровень `product`: сборка+запуск+приёмка (smoke/e2e/metrics) | **[своё]** `product_check` + **[Hermes]** terminal/worktree |
| D4 | contract_check против РАБОТАЮЩЕГО API (не манифеста) | **[либа]** specmatic (mock/contract test) + **[своё]** обвязка |
| D5 | Acceptance-схема в кейсе (entrypoint/smoke/e2e/metrics) | **[своё]** схема YAML |

### E. Самосовершенствование (новое — замыкание ленты ревизии на продукт)

| # | Функция | Средства |
|--:|---|---|
| E1 | Сигналы: ошибки приёмки, m тестовых ошибок, деградация метрик, смена правил среды | **[своё]** `research_trigger_check` (расширить источники) |
| E2 | Цикл сигнал→ревизия→respec→re-derive→execute→повторная приёмка | **[своё]** respec-gate + раннер |
| E3 | Анти-зацикливание: бюджет переоткрытий, порог доказательств, ручное подтверждение при большом радиусе | ✅ есть **[своё]** respec-gate |
| E4 | Главный тест: довести до ГОТОВ → сломать среду → плагин сам возвращает в зелёное за ≤N итераций | **[своё]** `test_self_improvement.py` |
| E5 | Durable-провенанс: superseded_by, история версий спек | **[Hermes]** Kanban runs + git; **[своё]** метаданные |

### F. Исполнение на Kanban (Hermes-нативное)

| # | Функция | Средства |
|--:|---|---|
| F1 | Дерево узлов = карточки на одной доске, parents→child | **[Hermes]** Kanban |
| F2 | Диспетчер спавнит свежего воркера на узел (глубина не ограничена 3) | **[Hermes]** gateway |
| F3 | block→unblock→re-run при respec; локализация радиуса по DAG | **[Hermes]** + **[своё]** respec-gate |
| F4 | human-in-the-loop где требует policy_gate (выплаты, рассылки) | **[Hermes]** + **[своё]** policy_gate |

---

## Порядок реализации (по ценности и зависимостям)

1. **D3+D5 (уровень `product` + acceptance-схема)** — [своё], без внешних
   зависимостей; даёт честный вердикт «НЕ ГОТОВ» с заглушкой. → корень
   проверки «готовый продукт».
2. **C1–C4 (FSM жизненного цикла узла)** — [движок], переиспользуем
   workflow-engine; инварианты становятся рантайм-гейтами. Опционально,
   дефолт остаётся inline.
3. **B1 (импорт spec-kit `tasks.md` → Kanban)** — [либа]+[своё]; первая
   связка со стандартом, самая дешёвая и востребованная.
4. **D2 (живой implementer)** — [либа]; начать с p2 (наименьший продукт).
5. **E1–E4 (самосовершенствование)** — [своё]; главный тест ценности
   плагина, после 1–4.
6. **B2/B3/B5 (Kiro/OpenSpec/openapi-mcp адаптеры)** — [либа]+[своё]; по
   мере спроса.

## Не делаем сами (берём готовое — закрепить решение)

- валидаторы контрактов → redocly/specmatic/tsc/buf;
- генерацию спек-артефактов → spec-kit/OpenSpec/Kiro как фронт;
- EARS-нотацию → как соглашение;
- веб-поиск/браузер для ресёрча → MCP-серверы Hermes;
- стейт-машину → engines/workflow-engine, не свой FSM;
- LLM-исполнителя/декомпозитора → Claude Agent SDK / Hermes-воркер.

## Принцип (зафиксировать в README)

spec-flow — это **движок дисциплины и durable-оркестрации**, а не генератор
спек и не валидатор. Всё, что уже решено опенсорсом (генерация артефактов,
валидация контрактов, EARS, FSM-рантайм, веб-поиск), подключается, а не
переписывается. Своё — только то, чего нет ни у кого: рекурсивные гейты
глубины, drift→respec, две линии ревизии, изоляция ролей и пересборка
поддерева по DAG.

---

# Пошаговый чеклист исполнения (хронологический)

Идём сверху вниз, отмечаем `[x]` по факту, переходим к следующему.

## Фаза 1 — каркас проверки (СДЕЛАНО)
- [x] Умный оракул: `tree` → инварианты исхода (опорные точки, границы глубины, эпизоды, петли, покрытие, ресёрч-до-кода). Перенесён в код плагина (`build_oracle_report`).
- [x] Глубина `product` (D3/D5): сборка+запуск+приёмка → `PRODUCT-RESULTS.md`, честный NOT READY со заглушкой.
- [x] Стейт-машина жизненного цикла узла (C1–C4) на `engines/workflow-engine` + fallback.
- [x] Сборка ветки `feat/roadmap-phase-1`, прогон p4 на `product`, все отчёты строит код плагина, 185 тестов.

## Фаза 2 — две методики ревизии (из conversation.md)
- [x] R-2a **внутренняя ревизия** (continuous): срабатывает ВО ВРЕМЯ прогона по накоплению (`every_n_tasks` / `m_test_errors`), может переоткрыть ветку на ходу. Метод `internal`, петля с `method=internal`.
- [x] R-2b **ревизия при завершении ветки и возврате на шаг вверх** (`on_level_return`): `_research_tick(node, depth)` срабатывает в момент, когда поддерево свернулось и управление вернулось выше; передеривирует затронутое поддерево. Метод `level_return`.
- [x] Поддержка `revisions:` (список, у каждой `method`) в кейсе; обратная совместимость со старым одиночным `revision:`.
- [x] Оракул: эпизоды `revision_internal` и `revision_level_return`; тесты обоих методов.

## Фаза 3 — HITL (человек в цикле)
- [x] Инжектируемая роль `approver` с автономным дефолтом; профиль `profiles/approver/`.
- [x] HITL по требованию политики: трата выше лимита (выплаты), рассылка без согласия, respec с большим радиусом — гейт зовёт `approver`, событие `hitl`, при отказе — петля `hitl-reject`.
- [x] HITL в процессе создания спеки: чекпойнт после требований (утверждение конституции человеком) и узлы с блоком `hitl:`.
- [x] HITL со стороны человека: инжектируемый approver может ОТКЛОНИТЬ → ветка возвращается на доработку (тест approve + reject).
- [x] Оракул: эпизод `hitl`; покрытие профилей с учётом `approver`.

## Фаза 4 — сборка и проверка на кейсе
- [x] p4 расширен: обе ревизии (`revisions:`) + HITL-узлы; прогон на глубине `product`.
- [x] Перегенерированы все отчёты (footprint, oracle, product, workflow, SUMMARY) — кодом плагина.
- [x] Полный набор тестов зелёный; коммит + пуш ветки.

## Фаза 5 — далее (после ревью)
- [x] Вшить FSM узла в раннер (`node_engine="fsm"`, дефолт `inline`); единый guard
      гейтов для обоих движков (`_NodeDriver`), общий список обязательных гейтов
      из `spec_flow_node_fsm` (GATES_LEAF/GATES_BRANCH).
- [x] **Отдельные тесты на два движка жизненного цикла узла** (`tests/test_node_engines.py`):
      прогон p1 и p4 с `node_engine="inline"` и `node_engine="fsm"` — исход, гейты,
      петли, покрытие и oracle совпадают; негативный тест (`_skip_gate`) — пропуск
      гейта ловится в обоих движках (GateViolation).
- [x] B1 импорт `tasks.md` от spec-kit → карточки Kanban (`parse_speckit_tasks`
      + тул `speckit_import`): фазы, маркер `[P]`, пути файлов, секция Dependencies
      (blocks/depends on/before + диапазоны Txxx-Tyyy). Парсер — чистый код,
      сидинг карточек деградирует без Hermes. `tests/test_speckit_import.py` (11).
- [ ] E замкнутый цикл самосовершенствования (сигнал→ревизия→respec→re-derive→приёмка).
- [ ] D2 живой implementer-агент (реальный код листа) — тратит реальную квоту,
      запускать только с подтверждения пользователя.
