# HANDOFF — 2026-07-03 (после v154) — цикл храповика

Сделано: v154 (первый прогон с чекпойнтами, S6.10 в лаунчере) дал ЧЕСТНЫЙ
NOT READY — новый гейт S10.17 поймал дубль владения (core+web_ui на 4
маршрутах). Разбор вскрыл корень: движок сам приказал web_ui строить чужие
обработчики — вывод владения хватал унаследованную прозу (Traces-to, «reusing
the existing /notes logic»). Закрыто (3876a04, ba6b3af, b9c299a, 7584735):
S10.19 владение из typed exposes / только своей прозы, чужой маршрут =
зависимость (first-owner-wins, событие «route dependency»); S10.20 чистка
выброшенных модулей долговечна (набор + санация данных узла + ретаргет на
двери записи спек); S10.21 чужая заявка = собственная веха FAIL + причина
доктору (раньше тонула обрезанной в card-gate и лечилась только доливкой).
Наборы 571 зелёных (3 старых — #150). Сводка v152:
tests/runs-out/2026-07-03T06-31-32__v152__p6-micro-notes/layers-summary.md.

Дальше: вести v155 (pid 3238504, лог tests/runs-out/_detached_20260703-093443.log,
контрольная точка 10:23): ждать READY без соперников (события «route
dependency (owned by core)»), чистые l0/product_entry (без db.py), снимки в
checkpoints/. На терминале: слой 1 CLI (consistency + journal_invariants) +
investigator.py по каталогу; сводку слоёв в каталог прогона. При провале —
цикл (+Z). Реплей: --from-run 155. Запуски только tests/run-detached.sh
(чекпойнты сами включаются).
Открытое: #150 parallel_children, #146 гонка волн p4, #139 dry-plan,
#109 tests/workers, #135/#136.
