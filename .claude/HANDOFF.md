# HANDOFF — 2026-06-30 — spec-flow: faulthandler + small-product floor (#122)

## Сделано (всё запушено в feat/roadmap-phase-1)
1. **faulthandler** (`ca94d8e`): прогон исчезал без следа (C-краш в потоке) →
   `faulthandler.enable()` + SIGTERM/SIGUSR1 в run_cases, `PYTHONFAULTHANDLER=1`
   в run-detached.sh. Теперь смерть оставляет стек всех потоков.
2. **Диагноз до корня (v124/v125):** провал был НЕ дубль-владение, а
   ПЕРЕ-ДРОБЛЕНИЕ — декомпозер крошил микро-сервис в 8 листьев с 3
   конкурирующими «целыми приложениями», точка входа хрупко не собиралась.
3. **Small-product floor #122** (`ecf9c8b` + fix `ad2c930`): продукт
   product-глубины с ≤ `_small_product_routes` (деф 5) маршрутами строится ОДНИМ
   атомарным листом. Предикат `_small_product_root` + явный override verdict=leaf
   в `_visit` (НЕ подделка метрик — честная политика, перебивает LOC-эвристику
   leaf_check для микро-сервиса). Движок сам синтезирует точку входа.
   - Офлайн 326 зелёных (10 тестов `tests/coverage/test_small_product_floor.py`).
   - **Живая валидация v127:** пол сработал (тик 9 «small product → single
     atomic leaf, 3 routes ≤ 5»); дерево 8 модулей→3, 21 задача→4; БАЗА КОРРЕКТНА
     (ручная загрузка: GET /health→200, POST /notes→200{id}, GET /notes→200{items}).

## Остался гэп (задача #123) — late-req × single-leaf
v127 финал FAILED по ОДНОЙ причине: поздняя HITL-просьба «GET /ui (HTML)» НЕ
сгенерировала `get_ui` ни в одном модуле → `app.py._ROUTES` проводит только 3
базовых → GET /ui честный 404 → NOT READY. Late-req создал шумовой
`_product_logic.py` вместо правки `l0.py`.

## Точный следующий шаг (#123)
Поздняя просьба о маршруте при single-leaf продукте → дорастить канон-хендлер
(`get_ui`, HTML) В ТОТ ЖЕ единственный модуль `l0.py` (Фаза 5/#118 атомарная
карточка + Фаза 1 привязка), не плодить параллельный модуль. Проверка:
отвязанный p6 → GET /ui → 200 HTML, продукт READY.

## Состояние
- Ветка `feat/roadmap-phase-1`, HEAD `ad2c930`, всё запушено. Офлайн 326 зелёных.
- Прогоны: v125 (до фикса, 8 модулей/343 вызова/FAILED),
  v127 (после, 3 модуля/база корректна/FAILED только на late /ui).
