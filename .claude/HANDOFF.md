# HANDOFF — 2026-07-02T18:15 — sid babc3939

Сделано:
1. v148 вскрыт: убит внешним SIGKILL (~17:41, без следа — не SIGTERM, faulthandler молчал; журналы чисты, не ООМ; совпало с /compact + перезапуском hermes). Лаунчер теперь с супервизором: пишет `python exited rc=N` / `KILLED by signal S`; отсутствие строки = снос группы. Аудит S6.8.
2. Указание оператора выполнено: параллелизм пересматривается ОНЛАЙН — `_should_fork` на всех окнах (children / late-req / re-poll, у последнего параллельного пути не было вовсе), depth_limit по умолчанию снят (ограничитель — семафор). Решения о развилке в trace («fork opened/declined»).
3. Аудит усилен: STAGE 9 «живость способности» (tests/audit/test_parallel_liveness.py) — структурное перекрытие потоков (валидно при провайдере в 1 полосу), инъекции-накопление, рекомпозиция вглубь. Все 3 красные на старом движке, зелёные на новом. Аудит 20 passed, широкие наборы 534 passed.
4. p6: mimo ведёт все тиры/стадии (sonnet запасной). Bifrost xiaomimimo: 2 разных ключа, веса 1.0/1.0, concurrency 16 — балансирует, чинить нечего.
5. Попутный фикс llm_backend: явно пустой fallback_models=[] больше не подменяется claude-дефолтом (конфиг лгал, тесты жгли подписку).
6. Коммит 6d34695 в spec-flow main.

Прогон: v149 живой — pid 533041, лог tests/runs-out/_detached_20260702-181245.log, каталог runs-out/2026-07-02T18-12-51__v149__p6-micro-notes, 3 автономные инъекции.

Дальше: следить за v149 до честного терминала (meta.json + строка rc в хвосте лога обязана появиться при любой смерти); проверить в trace «fork opened at re-poll/late-req» и перекрытие по llm-log (mimo параллелится). При провале — разбор → новое правило аудита (+Z) → фикс → аудит → перезапуск.

Открыто: #146 гонка research_before_impl (межуровневые зависимости в волнах, S9.4), аудит-ступени 5/7/1, уровень В dry-plan (#139), #135/#136.
