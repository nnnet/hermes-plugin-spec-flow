# HANDOFF — spec-flow — feat/roadmap-phase-1

## Системные фиксы этой сессии (офлайн 490 зелёных, запушено ad356c4)
Каждый — отдельный КОРЕНЬ, независимый от кейса; каждый сдвигал живой провал
p6 ВПЕРЁД (не лестница симптомов — разные классы):
1. **C3-C5** (`d73359d`) — движок медиум-независим (не-веб продукт: контракт по
   `_project_kind`, `_synthesize_lib_entry`, `_nonweb_capability_boots`).
2. **Роутер 4xx-не-5xx** (`e8e1f8b`) — пустое write-тело→400, `KeyError`→400,
   прочее→500. Снял блокер v141 (empty-body 500).
3. **429-квота-брейкер** (`ad356c4`) — исчерпанную free-модель ретайрим в
   `_MODEL_DOWN` после `model_breaker_429` (4) кумулятивных 429 (любой
   QuotaExhausted, вкл. free-кулдаун, по типу); аварийный подбор ранжирует по
   5xx+429, claude-ротация — гарантированный резерв; фикстура сбрасывает health.
   ВСЕ роли (tester/coder/…) уходят на claude-via-Meridian когда free мёртв.
   Снял v142-корень (слабая модель писала тест↔хендлер 201/200 — на claude
   рассогласование исчезло).
4. **Инфра**: bifrost anthropic→Meridian :3456 (`a681e62`), таймаут 120→300
   (`7e4508a`). Дашборд :8092 поднят. claude/sonnet→Meridian доказано.

## Живая прогрессия p6 (провал двигается к концу)
- v141 FAILED: empty-body 500 → фикс #2.
- v142 FAILED: тест↔хендлер 201/200 (слабая модель) → фикс #3 (claude).
- **v143 FAILED, но дальше всех**: claude вынес 17+ вызовов, **исходный продукт
  ЗЕЛЁНЫЙ** (health/notes/ui/about, e2e green, вход синтезирован), падение только
  на ПОЗДНЕЙ инъекции `delete_note`: 3 проваленных чека + дубль-владение
  маршрутом (6 листьев на POST /notes) + промежуточный `ImportError: delete_note`
  (в финале символ уже в core.py:35 — доктор переносил, но не сошёлся к капу
  реворков, treatments=13).

## СЛЕД. СИСТЕМНЫЙ КОРЕНЬ (не патч — обсудить с юзером)
**Позднее требование → чистая атомарная интеграция** (область #113, pending):
поздняя инъекция (delete_note) строится, но её ВЛАДЕНИЕ маршрутом и привязка
символ↔импорт↔тест не сходятся детерминированно — тот же single-source-контракт,
что для success-кода, но для late-req: карточка листа поздней просьбы пиннит
{экспортируемый символ, владелец маршрута, где импортируется}, гейт проверяет
до сборки (как C2 для рёбер). Плюс дубль-владение маршрутом (6 листьев) —
декомпозер плодит владельцев на позднюю просьбу вместо единственного (нарушение
#122 для LATE-инъекций).

## Воспроизведение
`bash tests/run-detached.sh --case p6_micro_notes --depth product --workers real --doctor-enabled`
claude через Meridian рабочий; free xiaomimimo то штормит 429 (429-брейкер уводит
на claude), то частично оживает. Дашборд http://localhost:8092/.
