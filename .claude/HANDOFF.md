# AUTO-COMPACT HANDOFF — 2026-07-02T16:45:36+03:00 — sid babc3939-4d3b-4fd6-be01-62e1daf43fde

Сделано: (1) выявил корень v146 — нет глобального бюджета прогона (per-node пределы есть, но их сумма не ограничена → 46 мин); (2) пропатчил движок: RunExhausted + RUN_CALL_BUDGET=220 + бюджет-прокси на все вызовы агентов + честный терминал NOT READY; импорт OK, офлайн 592 passed без регрессов (красные только предсущ.); (3) сохранил состояние в HANDOFF.md.
Дальше: (1) `pytest tests/audit` — подтвердить зелёный на патче; (2) добавить ступень 6 аудита (test_termination_invariants.py: бюджеты конечны>0) + ступень 8 доп-тест (вечный реворк при малом run_call_budget → терминация через RunExhausted, ловец v146 офлайн); (3) закоммитить бюджет+аудит в spec-flow; (4) гейт-прогон p6 через tests/run-detached.sh (аудит зелёный→старт), ждём честный терминал вместо 46-мин зависания; (5) провал→новое правило аудита(+Z)→фикс→снова аудит. Всё чисто на Fable 5 (на Opus был баг утечки тегов вызова).
