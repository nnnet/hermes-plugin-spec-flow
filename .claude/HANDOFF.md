# HANDOFF — spec-flow — feat/roadmap-phase-1

## Сделано в этой сессии (закоммичено + офлайн 488 зелёных, запушено e8e1f8b)
- **C3-C5** (`d73359d`) — движок медиум-независим: не-веб продукт (lib/cli) доходит
  до контракта/сборки/приёмки. `_product_contract` по `_project_kind` отдаёт
  `{kind,entry,exposes}` без routes; `_try_synthesize_lib_entry` собирает вход
  реэкспортом API; `_nonweb_capability_boots` судит по поведению. Веб не тронут.
- **Роутер: ошибка ввода клиента → 4xx, не 5xx** (`e8e1f8b`) — детерминированный
  WSGI-роутер ловил только битый JSON→400. Пустое тело на write и `KeyError`
  (нет обязательного поля) проскакивали → слабый хендлер `payload['text']` →
  500. Теперь: пустое write-тело→400, KeyError→400, прочее→500. Класс-уровень.
- **Инфра (родит. repo, ветка feat/bifrost-gateway):** bifrost anthropic →
  Meridian :3456 (`a681e62`, был мёртвый :8791); таймаут 120→300с (`7e4508a`,
  claude-кодоген >120с давал 504→ретайр claude). claude/sonnet→Meridian доказано
  меткой ROUTEPROOF в логе Meridian. Дашборд :8092 поднят отвязанно.

## Живые прогоны сегодня — все FAILED честно (сигнал сдвигается вперёд)
- v140 деградировал (claude 504 до фикса таймаута) — неинформативен.
- v141 FAILED: `test_empty_body_on_post_400: assert 500==400` → корень найден,
  пофикшен в роутере (см. выше).
- v142 FAILED (после фикса): блокер СДВИНУЛСЯ на **новый класс** —
  `test_post_notes_returns_id: assert 201==200` + недостроенный `delete_note`.
  Слабая free-модель пишет ТЕСТ (ждёт 201) и ХЕНДЛЕР (отдаёт 200), которые
  расходятся по коду, не пиннутому спекой; доктор их не мирит, особенно с
  tester'ом, голодным от 429 (free-квота xiaomimimo исчерпана за день).

## СЛЕД. ШАГ (структурный, НЕ реактивный патч под статус-код)
Два прогона подряд — два расхождения по коду (500vs400, 200vs201). Не клеить
третий костыль. Настоящий корень: **тест и хендлер выводятся из ОДНОГО
пиннутого success-контракта**, чтобы не могли разойтись (success-код +
форма тела фиксируются в карточке листа / контракте, оба потребителя читают
одно). Плюс tester вне исчерпанного free-пула (fallback на claude/подписку при
429), иначе живой сигнал шумный. Обсудить с юзером ПЕРЕД кодингом — это дизайн,
не правка.

## Как воспроизвести
`bash tests/run-detached.sh --case p6_micro_notes --depth product --workers real --doctor-enabled`
(free-квота xiaomimimo сбрасывается посуточно; claude через Meridian рабочий).
Дашборд: http://localhost:8092/ (авто-следит за последним прогоном).
