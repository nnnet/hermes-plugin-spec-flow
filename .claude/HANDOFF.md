# HANDOFF — 2026-07-03 (после v155) — цикл храповика

Сделано: v154 (первый прогон с чекпойнтами, S6.10 в лаунчере) дал ЧЕСТНЫЙ
NOT READY — новый гейт S10.17 поймал дубль владения (core+web_ui на 4
маршрутах). Разбор вскрыл корень: движок сам приказал web_ui строить чужие
обработчики — вывод владения хватал унаследованную прозу (Traces-to, «reusing
the existing /notes logic»). Закрыто (3876a04, ba6b3af, b9c299a, 7584735):
S10.19 владение из typed exposes / только своей прозы, чужой маршрут =
зависимость (first-owner-wins, событие «route dependency»); S10.20 чистка
выброшенных модулей долговечна (набор + санация данных узла + ретаргет на
двери записи спек); S10.21 чужая заявка = собственная веха FAIL + причина
доктору (раньше тонула обрезанной в card-gate и лечилась только доливкой).
Наборы 571 зелёных (3 старых — #150). Сводка v152:
tests/runs-out/2026-07-03T06-31-32__v152__p6-micro-notes/layers-summary.md.

Дальше: вести v155 (pid 3238504, лог tests/runs-out/_detached_20260703-093443.log,
контрольная точка 10:23): ждать READY без соперников (события «route
dependency (owned by core)»), чистые l0/product_entry (без db.py), снимки в
checkpoints/. На терминале: слой 1 CLI (consistency + journal_invariants) +
investigator.py по каталогу; сводку слоёв в каталог прогона. При провале —
цикл (+Z). Реплей: --from-run 155. Запуски только tests/run-detached.sh
(чекпойнты сами включаются).
Открытое: #150 parallel_children, #146 гонка волн p4, #139 dry-plan,
#109 tests/workers, #135/#136.

## v155 (после v154-блока)
v155 честный NOT READY: ревьюер дважды отверг L0-спеку за РЕАЛЬНОЕ
противоречие конституции («storage through sqlite3 in src/db.py» vs
схлопывание в core.py) — чистка S10.20 сняла фиговый листок, v152 нарушал
это же правило втихую. Закрыто S10.22 (37e86ce, 1e5205d, b2ae6bd):
пути из конституции — неприкосновенные данные; извлекатель
_constitution_pinned_paths, схлопывание оставляет закреплённому модулю
свой лист (план p6: db + core + синтез app.py), чистка не ретаргетит
закреплённое, план-гейт краснеет на закреплённом пути без владельца.
Наборы 575 зелёных (3 старых — #150).

Дальше: вести v156 (pid 3413915, лог tests/runs-out/_detached_20260703-103225.log,
точка 11:21): ждать лист-владелец src/db.py, без осцилляции ревьюера,
«route dependency (owned by core)». На терминале: слой 1 CLI +
investigator.py + сводка слоёв. При провале — цикл (+Z). Реплей: --from-run 156.

## v156 (терминал 11:45, честный NOT READY на интеграции)
Хорошее (не регрессировать): src/db.py построен как закреплённый лист
(S10.22 работает), ревьюер без осцилляции, «route dependency (owned by
core)» вместо соперников. Провайдерная деградация в середине (таймауты
mimo + один auth-сбой клода) — инфраструктурный шум, не движок.
Провал: 17 красных чеков — (1) test_app.py (тест точки входа) ждёт 200
на POST при контракте 201 — гейт статусов освобождает вход целиком;
(2) поздний DELETE не попал ни в контракт, ни в маршрутизацию (405);
(3) смоук /health красный (причина не установлена); открытые причины
web_ui:empty_delta + task_check_mismatch. Разбор+фиксы у фонового
агента-инженера (S10.23+). После его отчёта: проверить наборы, коммиты,
сводку слоёв в каталог v156, перезапуск — провал поздний, честен реплей
`--from-run 156` (фикс DEFECT 2 меняет контракт → лучше с нуля, решить
по объёму фикса). Реестр точек: провайдерная линия отдельно от движка.

## v156 разбор завершён — S10.23-S10.26 (9 коммитов, 594 зелёных)
1. S10.23: гейт статусов был пустышкой для листа входа (code_target →
   owned=[]) — теперь вход обязан утверждать контрактные статусы всех
   маршрутов. 2. S10.24: поздний маршрут рождается как ДАННОЕ
   (_late_req_bound_route: путь из владений цели правки, метод из глаголов
   требования) → binds_route → контракт+вход. 3. S10.25: фантомный символ
   зависимости (db.list_notes при живом get_notes) краснеет на двери
   записи — корень смоук-провала /health (GET /notes 500 валил общий
   оракул). 4. S10.26: гейт дельты сверяет данное о владении, не прозу
   соседей (web_ui:empty_delta). Слой 1 по v156: 3 находки — все совпали
   с фиксами. Следователь по v156 работает в фоне — дочитать
   investigation.md на точке v157.
Дальше: вести v157 (pid 3755360, лог tests/runs-out/_detached_20260703-121848.log,
точка 13:04). При READY — первый честный зелёный. При провале — цикл (+Z).

## v157 — мета-вывод (вопрос юзера «что делаешь не так»)
v157 NOT READY: core.py `from db import store_note` — символа нет в db.py
(S10.25 сознательно пропускал from-import). Признано: v149/v150/v156/v157 —
ОДИН мета-класс «договор двух модулей не существует как данное до работы»;
я чинил проявления по одному вместо класса классов. Флагманский фикс у
фонового инженера — СТУПЕНЬ 11: межмодульный договор символов как данные
(из exposes → contracts/modules.json → печать в привязки ОБЕИМ сторонам →
два гейта на двери: экспортёр обязан определить, импортёр может брать
только договорное; реворк перепечатывает договор). Это обобщение решения
для маршрутов на весь мета-класс.
Дальше: дождаться отчёта агента (S11) → проверить наборы → коммиты →
запуск v158 → при провале цикл (+Z). Следователь по v156 в фоне —
дочитать investigation.md.

## Ступень 11 влита (7 коммитов, 609 зелёных) — v158 запущен
Межмодульный договор символов как данные: датум из exposes/текста узла →
contracts/modules.json → печать в привязки поставщику («обязан определить
ровно») и потребителям («ввозить можно ТОЛЬКО») → два затвора на двери
записи (по датуму, порядок сборки не важен; льгота from-import из S10.25
закрыта; дословный v157 не может дожить до сборки) → реворк перепечатывает
замороженную поверхность. Честно-красный путь: поставщик без выводимой
поверхности — красная карточка с требованием exposes.
Дальше: вести v158 (pid 4016716, лог tests/runs-out/_detached_20260703-132946.log,
точка 14:17). Ждать READY — первый честный зелёный (сводка + предложить
пуш). При провале — цикл (+Z). Хвосты в #171: зомби _product_logic.py,
метрики-vs-Scope при пине, следователь-v156 находки 3-4.

## v158 терминал (14:49, rc=0) — ближайший к зелёному
Смоук+e2e зелёные, S11 держит (modules.json, ноль ImportError). Красна
ТОЛЬКО интеграция: 2 теста core требуют HTML от GET /notes (контракт —
JSON; класс v152 на тестовой стороне) + открытые причины
about_page:empty_delta и weak_implementer. Слой 1: единственная находка —
зомби _product_logic.py без владельца (повтор v157). Фоновый инженер чинит:
S10.27 гейт формы тела (тест против контрактного JSON/HTML), S10.28 разбор
empty_delta about_page, S10.29 зомби-писатель _product_logic, + оценка
находок 3-4 следователя v156. После отчёта: наборы → коммиты → v159.

## v158 разбор закрыт — S10.27-S10.29 (4 коммита, 625 зелёных) — v159 запущен
1. S10.27 media-датум: форма тела маршрута — данные (_route_media_map, поле
   media в interface.json); тест листа с HTML на JSON-маршруте краснеет на
   том же шве, что статусы. 2. S10.28 корень empty_delta: провайдер вернул
   content-parts списком → AttributeError мимо всех except → оркестр записал
   ноль файлов; нормализация _message_text в llm_backend (+FakeOpenAI тест).
3. S10.29 зомби: писатель — _harvest_entry_handlers; владение через
   artifacts узла входа + чистка устаревшего урожая. Находки 3-4 v156 —
   покрыты (S10.22/Фаза 7), задокументировано. #171 закрыть после v159.
Дальше: вести v159 (pid 155237, лог tests/runs-out/_detached_20260703-154415.log,
точка 16:33). При READY — первый честный зелёный (сводка + предложить пуш).
Известно: 22 падения tests/workers — старые, база a9c9b04 (#109).

## v159 разбор закрыт — СТУПЕНЬ 12 (3 коммита 83c965e/3058285/84ec5fe, 649 зелёных) — v160 запущен
S12.1 форма запроса + переменные окружения = данные (request_fields в
interface.json, _constitution_env_vars; «NOTES_DB — не поле запроса» в
привязке; ворота: обязательные поля обработчика ⊆ контракта). S12.2 корень
пустой дельты: /about ПРИЗЕМЛИЛСЯ, а реворк доктора переписал core.py
выкинув get_about/delete_notes — теперь дверь отказывает стиранию
контрактных обработчиков (_route_handler_erasure), реворк перепечатывает
поверхность маршрутов. S12.3 необслуженный объявленный маршрут — именованный
FAIL на ПЕРВОЙ сборке (_unserved_route_gate). Не провайдер!
Дальше: вести v160 (pid 466225, лог tests/runs-out/_detached_20260703-173118.log,
точка 18:21). При READY — первый честный зелёный (сводка + предложить пуш).
Прогресс прогонов: v157 импорты → v158 два теста формы → v159 продукт
грузится → v160 ждём зелёный. Хвост: #171 (метрики vs Scope), #109 workers.

## v160 (16 мин, честный красный) — ПРОДУКТ БЫЛ ПОЛНОСТЬЮ ЗЕЛЁНЫЙ
В провалах только запись [integrate]: смоук/e2e/свод — чистые. Держала
красным одна бухгалтерская запись: находка гейта статусов (размазанный
[200,201], событие 128) получила ЧУЖОЙ ярлык empty_delta (129), реворк
прошёл, узел DONE (138), а причина не закрылась (145). Все новые ворота
S12 сработали вживую (request-shape поймал поле вне контракта на /ping).
Фоновый инженер чинит: S12.4 причина именуется своим гейтом (не чужое
ведро), S12.5 причины закрываются привязываемо (перепроверка открывшего
гейта на DONE; не чинилось — узел не DONE). После отчёта: наборы →
коммиты → v161 — по всем данным следующий прогон должен быть ПЕРВЫМ
ЧЕСТНЫМ ЗЕЛЁНЫМ (сводка + предложить юзеру пуш).

## S12.4-S12.5 влиты (0412e9e, 89b636d; 656 зелёных) — v161 запущен
Причины доктора: именование по открывшим воротам (_gate_cause в
spec_flow_diagnosers; empty_delta только для delta-ворот и пустых
поставок) + привязываемое закрытие (_doctor_advise пишет ворота в open,
_prune_stale_causes на оценке корня тихо перепрогоняет их по текущим
артефактам: чисто → «cause resolved: <ворота> re-ran clean», красно →
честно держит). Хранение: _doctor_states[nid] = {last_cause, open, recheck}.
Дальше: вести v161 (pid 632203, лог tests/runs-out/_detached_20260703-183820.log,
точка 19:27) — ожидается ПЕРВЫЙ ЧЕСТНЫЙ ЗЕЛЁНЫЙ (v160 продукт уже был
зелёным, держала одна незакрытая запись гроссбуха). При READY: слой 1 +
сводка + веха + предложить пуш. При провале — цикл (+Z).

## v161 (терминал 19:18, честный красный) — гроссбух работает, два новых класса
S12.4 подтверждён вживую: причины названы верно (web_ui:handler,
product_entry:test_status), empty_delta-подмен нет. Но: (1) класс
NOTES_DB-в-теле вернулся в НОВОЙ форме — общая обёртка валидации даёт 400
даже на GET /ui; AST-маркеры S12.1 эту форму не видят → нужен
ПОВЕДЕНЧЕСКИЙ гейт: живая проба каждого маршрута контрактным телом на
первой сборке (S12.6); (2) размазанный assert [200,201] пережил реворк
ВТОРОЙ прогон подряд — механически чинибельно движком (автофикс AST с
событием, S12.7); (3) события «cause resolved» в трейсе нет — проверить,
запускалась ли перепроверка S12.5 вообще (S12.8). Фоновый инженер чинит.
Дальше: после отчёта — наборы → коммиты → v162. Продукт в v160 уже был
зелёным целиком; v161 сломала слабая модель на реворке — гейты сжимают
кольцо.

## S12.6-S12.8 влиты (35face3, ea8edfb, 319a35e, 9f70607; 670 зелёных) — v162 запущен
Корень v161 ошеломил: НЕ модель — СОБСТВЕННЫЙ синтезированный роутер движка
превращал KeyError от os.environ['NOTES_DB'] в 400 «missing required field»
на каждом маршруте, а загрузочная проба сама маскировала это через
setdefault. Фиксы: S12.6 поведенческая проба контрактных форм БЕЗ
env-переменных (восстановление в finally); S12.7 механический автофикс
размазанных статус-assert'ов (AST-хирургия + событие test_status_autofix;
несодержащий контрактный статус — честный красный); S12.8 перепроверка
причин была per-gate — handler_gate не регистрировал recheck (web_ui:handler
висел над починенным узлом), теперь зарегистрирован; test_status recheck
держал ЧЕСТНО (смазка реально была в финальном артефакте — закрыто S12.7).
Правило на будущее: новый гейт с _doctor_advise ОБЯЗАН регистрировать
_register_gate_recheck (quiet=True), иначе его причина не закрывается.
Дальше: вести v162 (pid 841586, лог tests/runs-out/_detached_20260703-195452.log,
точка 20:44) — ожидается ПЕРВЫЙ ЧЕСТНЫЙ ЗЕЛЁНЫЙ. При READY: слой 1 +
сводка + веха + предложить пуш. При провале — цикл (+Z).

## v162 (терминал 21:03, честный красный) — молчащие ворота
Живой продукт: /health 200, но POST/GET /notes → 400 NOTES_DB (класс
отмытого KeyError ЖИВ), /ui и /about → 404. При этом S12.6 проба и S12.3
быстрый провал НЕ дали ни одной находки за прогон — ворота молчат на
реальном пути сборки (гипотезы: пустой датум request_fields → тихий
пропуск; путь сборки мимо барьера; env не убраны). Плюс доктор 3 раза
винил core за тесты /about и /ui (чужие поверхности — виноват выбор
обвиняемого) и «GET /about: no owner leaf» в финале. Фоновый инженер:
S12.9 ворота обязаны стрелять на этом пути + пропуск-с-событием +
пустой датум для POST с полями в цели = красный; S12.10 обвинение по
датуму владения маршрута падающего теста; S12.11 куда делось владение
about_page/web_ui в этом прогоне. После отчёта: наборы → коммиты → v163.

## S12.9-S12.11 влиты (5c553ad, 3dd9372, 153ee24; 690 зелёных) — v163 запущен
Корни v162: (1) проба формы стояла ПОСЛЕДНЕЙ в fail-fast загрузочной пробе
— умирала на 404 /ui раньше очереди → перенесена в голову + анти-тишина
(пропуск = событие, невыведенная форма при человеческом тексте = красный);
(2) ворота необслуженных маршрутов стреляли один раз ДО первого свода — а
реворк раунда 1 стёр обработчики ПОСЛЕ → теперь перевызов после каждого
пересинтеза входа с пере-взводом дедупа; (3) обвинение грепало весь вывод
pytest и не знало владельцев необслуженных маршрутов → голосование по
владельцам маршрутов ПАДАЮЩИХ тестов; (4) /about-класс: буквальный НОВЫЙ
путь в поздней просьбе не привязывался (только новый метод на своём пути)
→ amend без поверхности → дверь не защищала → реворк стёр. Теперь
буквальный объявленный невладеемый путь привязывается первым, владение
регистрируется при прикреплении. Доказано на артефакте v162.
Дальше: вести v163 (pid 1139234, лог tests/runs-out/_detached_20260703-215254.log,
точка 22:42) — цепь замкнута, ожидается ПЕРВЫЙ ЧЕСТНЫЙ ЗЕЛЁНЫЙ. При READY:
слой 1 + сводка + веха + предложить пуш. При провале — цикл (+Z).
Мусор: notes.db в корне репо (от ручной пробы) — удалить при уборке.

## v163 (терминал 22:37) — ЖИВОЙ ПРОДУКТ ВПЕРВЫЕ ПОЛНОСТЬЮ РАБОТАЕТ
Проба: /health 200 json, POST /notes 201 {id}, GET /notes 200 items,
/ui 200 HTML, /about 200 HTML — все классы v149-v162 исчезли. Красным
держит ОДНА честная, но неверно адресованная находка: тест core шлёт
мусорное поле 'irrelevant' в GET /about (форма []) — ворота поймали
(событие 126), причину повесили на about_page (владельца МАРШРУТА), а
чинить надо файл core → реворк адресата не может закрыть причину.
Фоновый инженер: S12.12 находка адресуется владельцу АРТЕФАКТА (маршрут —
контекст) + перепроверка по правильному узлу; S12.13 механический автофикс
мусорного поля при пустой контрактной форме (аналог S12.7). После отчёта:
наборы → коммиты → v164 — ожидается ПЕРВЫЙ ЧЕСТНЫЙ ЗЕЛЁНЫЙ.

## S12.12-S12.13 влиты (f36a27e, 77432ad, 1e7a8af; 704 зелёных) — v164 запущен
Находки шлюзов адресуются владельцу АРТЕФАКТА (_artifact_owner_nid:
test_<m>.py → лист m; причина/цикл/перепроверка на адресате; тот же дефект
был и у шлюзов статуса и формы тела — исправлены все); мусорное поле при
пустой контрактной форме чинится механически (_strip_junk_payload_dicts,
только каноничная форма, событие request_shape_autofix; неоднозначное —
честный красный).
Дальше: вести v164 (pid 1314704, лог tests/runs-out/_detached_20260703-230128.log,
точка 23:51) — ожидается ПЕРВЫЙ ЧЕСТНЫЙ ЗЕЛЁНЫЙ (в v163 продукт уже был
живым целиком, держала неверная адресация). При READY: слой 1 + сводка +
веха + предложить пуш. При провале — цикл (+Z).

## v164 (терминал 23:46, честный красный) — прачечная найдена в движке
Проба: /health 200, но POST/GET /notes и /ui → 400 NOTES_DB, /about → JSON
вместо контрактного HTML. ГЛАВНОЕ: класс NOTES_DB отмывает СОБСТВЕННЫЙ
синтезированный роутер движка — любой KeyError обработчика (включая
os.environ) переводится в 400 «missing required field». Мы гоняли класс
через кодеров (S12.1) и пробы (S12.6/S12.9), а перевод — детерминированный
код движка. Фоновый инженер: S12.14 роутер-шаблон честен (исключение из
обработчика = 500 с типом, 400 только за кривое тело/явный возврат);
S12.15 поведенческая проверка media живого ответа (HTML-маршрут обязан
отдавать HTML); S12.16 директива реворка несёт контрактный media.
После отчёта: наборы → коммиты → v165.

## ПЕРЕОСМЫСЛЕНИЕ (2026-07-04, директива юзера) — курс изменён
Цели уточнены: универсальный софт без памяти прошлых проектов, спеки —
единственный источник, независимость от мощности модели. Кэш/
инкрементальность отвергнуты (память = костыль). Корневая ошибка
текущей архитектуры: ЛЛМ размазана по стадиям, стадии общаются прозой,
все ~30 правил аудита — один класс «рассогласование незакреплённого
значения». Новый курс: спека-IR + движок-компилятор — план в
.claude/plans/2026-07-04T00-45__spec-ir-compiler-rearchitecture.md
(фазы A-F, каждая по храповику).
Дальше (после 8:00, сброс лимита подписки): (1) перезапустить упавшего
по лимиту агента S12.14-16 (роутер-прачечная — вошло в Фазу C);
(2) начать Фазу A (IR-схема, слияние датумов, замкнутый мир);
прогоны p6 до Фазы B не гонять — сначала снять тестера с интерфейса.

## S12.14-S12.16 влиты (3f7cf11, ea2745f, 5dfd38d; 716 зелёных) — v164 закрыт
Роутер-шаблон честен: «missing required field» 400 может назвать только
контрактное поле (_REQUIRED из датума S12.1), любое исключение обработчика
= честный 500 с типом; boot-проба судит media поведенчески (route_media
в _assembled_product_boots; судится только контрактно объявленный media);
директива реворка печатает контрактный media на маршрут (html — «страница,
не JSON-словарь»). Попутный корень: окно вывода media в _product_contract
уважает границу «;»/новой строки (голос скобки протекал в соседний пункт).
Красные: test_router_exception_honesty, test_boot_media_probe,
test_rework_media_binding. Красные только 3 старых parallel_children (#150).
Урок витка: прачечная была ДЕТЕРМИНИРОВАННЫМ кодом движка — прямое
доказательство курса IR (ревизия движковых шаблонов обязательна).
Дальше: Фаза A плана spec-ir-compiler-rearchitecture — IR-схема
(интерфейс = настоящий OpenAPI 3.1, сценарии G-W-T первым классом,
слияние interface/modules/request_fields/media/env в замкнутый мир на
узел, валидатор); по храповику — красные правила до кода. p6 не гонять
до Фазы B.
