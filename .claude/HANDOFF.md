# HANDOFF — 2026-06-30 (вечер) — #127 /ui сирота ЗАКРЫТ (фикс `7640016`)

## #127 — re-poll late-req на root-integrate (ЗАШИПЛЕН + ДОКАЗАН)
**Корень /ui сироты:** движок читал standing requirements ОДИН раз — окно
размещения детей корня, ДО engine-сборки `product_entry`. В product+floor
(быстрый core-лист) web_ui приходил от HITL-диспетчера на trace-событии сборки
ПОСЛЕ окна → сирота (/ui в контракте, листа-владельца нет → 404).
**Фикс (`spec_flow_runner.py`, `_visit` branch-arm, depth==0 перед EV_BRANCH_INTEGRATE):**
перечитывать `_requirement_nodes()` на root-integrate и материализовать
непокрытое (bounded loop, не one-shot). uncovered == не в self.tasks — гейты НЕ
ослаблены; в норме no-op (p4/p5 целы).
**Доказано:**
- Офлайн-репро `tests/coverage/test_late_req_product_floor.py`: floor→core+product_entry,
  web_ui на узле сборки. RED→GREEN. Набор **340 зелёных**.
- Живьём v133 (p6 product/real/doctor): web_ui И второе требование `красивый_вид`
  СТАЛИ листьями (decompose→leaf_check→leaf, review PASS, implement→solo coder) —
  в v131/v132 сюда не доходили (сироты). Фикс материализации работает.
**Остаток до зелёного /ui — ТОЛЬКО #83:** v133 завис на mimo-v2.5 таймаутах
(под-шаг tester; 24 таймаута ≈30 мин чистого ожидания за 43 мин), файлы листьев
не легли — буря провайдера сильнее v131/v132. Сервинг /ui гейтит здоровье
провайдера, не движок. v133 оставлен в фоне (может досойтись).

## СЛЕД. ШАГ — #83 (ведущая модель / таймауты)
mimo-v2.5 системно таймаутит на tester-под-шаге. fast-fail брейкер (#102) есть,
но per-step цепочка всё равно ведёт mimo. Решить ведущую модель decompose/implement
(claude/sonnet подписочный стабилен, но тоже под нагрузкой). Без здорового
провайдера живой p6 не зеленеет — офлайн+структурный сигнал достаточен для движка.

---

# HANDOFF — 2026-06-30 — spec-flow: 4 структурных фикса (#122-#124) + Шаг 1 оси #82

## ИТОГ СЕССИИ 2026-06-30 (3 движковых фикса, все зелёные + запушены)
1. **#82 автофикс stdlib-теста** (`6205999`): свой `src/*.py` не клеймится сторонним.
2. **#83 coder-empty-retry** (`fe573b9`): пустой ответ кодера (таймаут) = провайдерный
   сбой → ретрай по цепочке до 3, даже ансамбль=1. Тела листьев теперь ложатся.
3. **Герметичный PYTHONPATH** (`4a27225`): продукт судится ТОЛЬКО по своему `src/`
   (`hermetic_env`); чужой одноимённый пакет (v131: `db` из другого репо) не затеняет
   и не даёт ложного зелёного. Все pytest-subprocess + boot-gate + лист-pytest.
Офлайн **338 зелёных**.

### v131 ИСХОД: база ПОЛНОСТЬЮ КОРРЕКТНА, остался ОДИН блокер — /ui
206 вызовов/30 мин (12 таймаутов провайдера — буря). Прямые WSGI-вызовы по финальному
продукту: **GET /health 200, POST /notes 200{id}, GET /notes 200{items}, POST пустое
→ 400** (баг пустого тела доктор закрыл сам). FAIL честный из-за **`/ui` 404**:
3 поздних HITL-инъекции (web_ui `GET /ui → HTML`) **НЕ материализовались в листья** —
5 «requirements» все базовые; `/ui` в контракте (integrate ловит «404/not HTML»), но
листа-владельца нет → сирота. Корень был замаскирован: boot-gate раньше падал на чужом
`db` (теперь исправлено герметичным PYTHONPATH).

### СЛЕД. ШАГ — /ui материализация (отдельный план, ТОЧНО локализовано)
См. `.claude/plans/2026-06-30T16-40__ui-late-req-materialization-floor-path.md`.
КОРЕНЬ: движок читает standing requirements ОДИН раз (root placement, `5510-5524`,
p6 yaml это документирует). В product+floor (один core-лист) окно проходит мимо
инъекции web_ui (диспетчер пишет в отдельном потоке по `event:"minimal impl"`) →
web_ui не материализуется (v131/v132 узлы: только core+product_entry; /ui в контракте
→ сирота 404). Офлайн depth=spec тест проходит → баг специфичен product/floor/live.
ФИКС: перечитывать standing reqs на root-integrate, материализовать непокрытое до
вердикта (не one-shot). Шаг 1 — офлайн-репро product+floor (красный). v131/v132 =
12 и 20 таймаутов провайдера (турбулентность, ось #83) — тоже ел бюджет.

## v130 ИСХОД (FAILED, но честно; новый блокер = таймаут провайдера, не #82)
106 вызовов (с 343 → пол+тиринг работают), единый базовый лист. Честный FAIL:
boot-gate поймал пустой продукт (`src/` = только синтез `app.py`, тел листьев нет;
ложного зелёного НЕТ). Корень в `orchestra_step_error`:
`node=core role=tester ERR=openai backend timed out after 75s` (и product_entry).
Кодер вернул ответ без файлов (деградация по таймауту), tester упал на 75с →
лист записал НОЛЬ файлов → пустой src → boot-gate RED. Транзиент провайдера
(как mimo-буря v091), ось #83. ЩЕЛЬ движка: кодер с нулём файлов молча
пропускается (leaf_small → ансамбль=1, ретрая нет) → пустой лист уходит в
integrate. СЛЕД. ШАГ: пустой ответ кодера = негодный кандидат → ретрай по
цепочке; ноль файлов после ретраев = лист RED (не молчком в integrate).

## ОБНОВЛЕНИЕ (Шаг 1 оси #82 ЗАШИПЛЕН, `6205999`, запушено)
`autofix_local_import_stdlib_test` в `pytest_verifier.py`: сгенерированный
«no third-party imports» тест клеймил СОБСТВЕННЫЙ модуль продукта (`app`) как
сторонний → ложный RED → доктор реворкал корректный код. Автофикс монотонно
дополняет allow-set теста реальными `src/*.py` (рантайм-открытие): pip-пакет так
не пройдёт (честно), а свой модуль больше не клеймится. Врезан в петлю
верификатора сразу за `autofix_non_ascii`. 6 юнит-тестов, офлайн **332 зелёных**.
Прогон v130 (p6 product/real/doctor) в полёте — проверяем, что доктор теперь
сходится на РЕАЛЬНОМ баге пустого тела (`POST /notes` пусто → 400) и доходит до
честного READY с поздним /ui.

---

# (история) HANDOFF — 4 структурных фикса (#122-#124) + диагноз до #82

## Сделано (всё запушено в feat/roadmap-phase-1)
1. **faulthandler** (`ca94d8e`): исчезавший прогон (C-краш в потоке) теперь
   оставляет стек всех потоков. `PYTHONFAULTHANDLER=1` в run-detached.sh.
2. **#122 small-product floor** (`ecf9c8b`+`ad2c930`): продукт ≤ N маршрутов
   (деф 5) собирается одним базовым модулем, не дробится в конкурирующие
   «целые приложения» (v125: 8 листьев/343 вызова/RED). Предикат
   `_small_product_root` + честный override (не подделка метрик).
3. **#123 корень остаётся ВЕТКОЙ** (`5b97654`): пол даёт ОДИН синтетический
   `core`-лист, корень не голый лист — иначе пропускался depth-0 блок поздних
   просьб+сборки (он в ветвевом пути `_visit`), и web_ui /ui не материализовался.
4. **#124 харвест-импорт нефатальный** (`344ff6d`): синтез точки входа делал
   `from _product_logic import *` ПЕРЕД хендлер-алиасами → сломанный харвест ронял
   импорт → NameError _h0 → продукт не грузился. Теперь try/except.
   Офлайн после всех: **326 зелёных**.

## Прогресс по живым прогонам
- v125 (до фиксов): 8 модулей, 343 вызова, RED (структурный хаос).
- v127 (после #122): 3 модуля, БАЗА КОРРЕКТНА (curl ОК), RED только на late /ui.
- v128 (после #123): корень-ветка + core-лист ✓, но застрял на NameError _h0.
- v129 (после #124): **NameError ушёл, продукт грузится и служит** (curl: /health
  200, POST/GET /notes 200). Дальше упёрлось в ОСЬ #82.

## Остался блокер — ОСЬ #82/#83 (качество кода/тестов слабой модели)
v129 не доходит до READY, потому что:
- РЕАЛЬНЫЙ баг кода: `post_notes` не отбивает пустое тело (`POST` пусто → 200
  вместо 400). Тест `test_empty_body_returns_400` ПРАВ — доктор законно реворкает.
- КРИВОЙ сгенерированный тест: `test_ac_l0_5_no_third_party_imports` ошибочно
  флагает локальный модуль `app` как сторонний импорт → ложный RED, гонит реворк.
Это не структура, а качество выхода слабой free-модели + хрупкость генерации
тестов. Отдельная большая ось (#82/#83), свежий контекст.

## Состояние
- Ветка `feat/roadmap-phase-1`, HEAD `344ff6d`, всё запушено. Офлайн 326 зелёных.
- 4 структурных/робастных фикса в деле; до зелёного p6 мешает #82.
